# eStore-API-Testing
End-to-end API test coverage for FakeStore API — a REST API simulating a real-world e-commerce backend. The suite validates functional correctness, response contract integrity, and negative/edge case handling across all four resource domains.
